//28 8 Palindrome or not without return type and arguments
#include<stdio.h>
#include<conio.h>

void Palindrome()
{
	int num,rem,reverse;
	for(int i=num;i<=num;i--)
	
	if(reverse==num){
		printf("\nNumber is Palindrome");
	}else
	{
		printf("\nNumber is not Palindrome");
	}
}

int main()
{
	Palindrome();
	getch();
}
